<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Images</title>

    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .image-container img {
            max-width: 80%;
            height: auto;
            margin-bottom: 15px;
        }

        .image-container2 img {
            max-width: 33%;
            height: auto;
            margin-bottom: 10px;
            padding: 10px;
        }

        .slide-container img {
            max-width: 50%;
            height: auto;
            padding: 10px;
        }

        h1 {
            /*font-size: 24px;*/
            /*margin-bottom: 20px;*/
            font-weight: bold;
        }

        .bg-light-custom {
            background-color: #f0f0f0;
            padding: 15px;
            border-radius: 8px;
        }

        .table img {
            max-width: 80%;
            height: auto;
            margin: 0 auto;
            display: block;
        }
    </style>
</head>

<body>
    <div class="container-fluid py-3">
        <div>
            <h3 class="mb-1 text-warning">ĐIỆN MÁY CHỢ LỚN - THÔNG TIN KHUYẾN MẠI</h3>
            <div class="py-1">
                 <h5> Thời gian kiểm tra: <%- vietnamTime %> </h5>
                <!-- <button type="submit" class="btn btn-primary" onclick="window.print()">Em ấn vào đây nhé</button> -->
                <i>Lưu ý: (ấn Ctrl + P để in hoặc lưu thành PDF)</i>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-4 mb-4">
                <div class="bg-light-custom text-center">
                    <h2 class="text-center text-info"> ẢNH Ở DƯỚI BANNER</h2>
                    <div class="image-container">
                        <%- imgTags %>
                    </div>
                </div>
                <div class="my-4">
                    <hr>
                </div>
                <div class="bg-light-custom">
                    <h2 class="text-center text-info">ƯU ĐÃI KHI THANH TOÁN</h2>
                    <div class="slide-container">
                        <%- imgTags3 %>
                    </div>
                </div>
            </div>

            <div class="col-lg-8 mb-4">
                <div class="bg-light-custom">
                    <h2 class="text-center text-info">ẢNH CAROUSEL</h2>
                    <div class="image-container2">
                        <%- imgTags2 %>
                    </div>
                </div>
                <div class="my-4">
                    <hr>
                </div>
                <div class="bg-light-custom">
                    <h2 class="text-center text-info">TIN KHUYẾN MẠI</h2>
                    <div class="image-container">
                        <table class="table table-striped table-bordered">
                            <thead class="table-primary">
                                <tr>
                                    <th>Ngày Tháng</th>
                                    <th>Tên Khuyến Mại</th>
                                    <th>Ảnh Khuyến Mại</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for(let i=0; i < ngayKhuyenMai.length; i++) { %>
                                    <tr>
                                        <td>
                                            <%= ngayKhuyenMai[i] %>
                                        </td>
                                        <td>
                                            <%= nameKhuyenMai[i] %>
                                        </td>
                                        <td>
                                            <% if(imgElementsKhuyenMai[i]) { %>
                                                <img src="<%= imgElementsKhuyenMai[i] %>" alt="Khuyến Mại">
                                                <% } else { %>
                                                    Không có ảnh
                                                    <% } %>
                                        </td>
                                    </tr>
                                    <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Simple script to cycle through slides
        let currentSlide = 0;
        const slides = document.querySelectorAll('.slide');

        function showSlide(index) {
            slides.forEach((slide, i) => {
                slide.classList.remove('active-slide');
                if (i === index) {
                    slide.classList.add('active-slide');
                }
            });
        }

        function nextSlide() {
            currentSlide = (currentSlide + 1) % slides.length;
            showSlide(currentSlide);
        }

        setInterval(nextSlide, 3000); // Change slide every 3 seconds
    </script>
</body>

</html>